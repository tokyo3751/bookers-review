<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/"><span>Bookers</span></a>
         <div class="collapse navbar-collapse" id="navbarNavDropdown">
           <ul class="navbar-nav ml-auto">
             <% if user_signed_in? %>
              <li>
                <div class="nav-link"><%= link_to "Home", user_path(current_user), class: "text-light" %></div>
              </li>
              <li>
                <div class="nav-link"><%= link_to "Users", users_path, class: "text-light" %></div>
              </li>
              <li>
                <div class="nav-link"><%= link_to "Books", books_path, class: "text-light" %></div>
              </li>
              <li>
                <div class="nav-link"><%= link_to "Log_out", destroy_user_session_path, method: :delete, class: "text-light" %></div>
              </li>
             <% else %>
              <li>
                <%= link_to "新規登録", new_user_registration_path %>
              </li>
              <li>
                <%= link_to "ログイン", new_user_session_path %>
              </li>
             <% end %>
            </ul>
          </div>
       </nav>
    </div>
  </header>